#lang sicp

;; The function a-plus-abs-b accepts two arguments.
;; It's body is an application, where the operator is an if-expression
;; If b is greater than zero, the if expression evaluates to +
;; Otherwise, it evaluates to minus
;; Thus the function returns a + |b|